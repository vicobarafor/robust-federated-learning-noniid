seed: 42

dataset:
  name: "MNIST"
  batch_size: 64
  eval_batch_size: 256

federated:
  num_clients: 20
  clients_per_round: 10
  rounds: 20
  local_epochs: 1
  lr: 0.01
  mu: 0.01

partition:
  type: "noniid"
  shards_per_client: 1

logging:
  out_dir: "results/sweeps/shard1_fedprox_mu001_e1"