seed: 42

dataset:
  name: "CIFAR10"
  batch_size: 64
  eval_batch_size: 256

federated:
  num_clients: 20
  clients_per_round: 10
  rounds: 8
  local_epochs: 1
  lr: 0.01

partition:
  type: "noniid"
  shards_per_client: 1

logging:
  out_dir: "results/cifar10_shard1_fedavg_sweep"