seed: 42

dataset:
  name: "MNIST"
  batch_size: 64
  eval_batch_size: 256

federated:
  num_clients: 20
  clients_per_round: 2
  rounds: 20
  local_epochs: 5
  lr: 0.01

partition:
  type: "noniid"
  shards_per_client: 1

logging:
  out_dir: "results/mnist_fedavg_noniid_shard1_drift"